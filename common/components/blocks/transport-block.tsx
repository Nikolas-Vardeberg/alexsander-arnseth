import { type TransportBlock } from "@/common/types/blocks/transportBlock.types"


const TransportBlock = ({ _type, moreLink, pages, variant, flipHorizontal, moreLinkTitle, options, title }: TransportBlock) => {
    return(
        <div>
            {title}
        </div>
    )
}

export default TransportBlock