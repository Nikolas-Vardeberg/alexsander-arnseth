
export type GlobalData = {
    navbar: Navbar;
    footer: Footer;
}

export type Navbar = {
    links: any[];
}

export type Footer = {
    facebookURL: string;
    instagramURL: string;
    tiktokURL: string;
}